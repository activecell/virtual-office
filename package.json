{
  "name": "Activecell Virtual Office",
  "version": "0.0.1",
  "description": "We'll see",
  "private": true,
  "scripts": {
    "test": "grunt && ./node_modules/testem/testem.js -f test/testem.json ci && grunt clean:ghpages"
  },
  "engines": {
    "node": "0.10.x"
  },
  "dependencies": {
    "grunt": "~0.4.1",
    "grunt-cli": "~0.1.9"
  },
  "devDependencies": {
    "testem": "~0.3.27"
  }
}
