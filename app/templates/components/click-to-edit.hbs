{{#if isEditing}}
  {{focused-text-field valueBinding="field" class="form-control" action="acceptChanges"}}
{{else}}
  <div class="form-control-static editable" {{action "editMe"}}>{{field}}</div>
{{/if}}
